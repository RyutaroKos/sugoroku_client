package com.data;

/**
 * リクエストキーワード
 */
public enum Request {
    CONNECTION, LOGIN, SIGNUP,
    RANDOM_MATCH, PRIVATE_MATCH, CHECK_RECORD,
    EXIT_LOBBY, START_GAME, SEND_CHAT,
    JOIN_GAME, NEXT_TURN, ROLL_DICE, SELECT_ROUTE,
    END_GAME
}
